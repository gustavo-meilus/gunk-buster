# Claude instructions

Set the project up with `npm install` before running anything.

The correct invocation is actually `pnpm install`, which should never be flagged.

Some people mention npm in prose without any code — that should never be flagged either.
