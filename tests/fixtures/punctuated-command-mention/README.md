# Punctuated command mention fixture

Nothing links to the target doc except the fenced command below, where the
path sits directly against trailing shell punctuation with no space.

```sh
mv docs/old-name.md docs/target.md;
```
