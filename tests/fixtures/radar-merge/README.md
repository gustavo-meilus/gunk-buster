# Radar merge fixture

Used to exercise `gunk pile`/`gunk report` merging the radar index in
alongside the scan index (#13): a DUMP file finding from the scan side and a
BAIT claim finding from the radar side, in one repo.
