# Protected files fixture

Every file here either is a code file or sits in a hard-protected location
(`.github/workflows/`, `migrations/`, `infra/`) — several of them also
match the DUMP generated-artifact pattern, to prove hard protection wins
outright and excludes them from candidacy before any detector runs.
