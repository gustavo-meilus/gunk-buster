# Workspace script doc

Run the app's dev server with `pnpm dev`.

You can also build the root package with `npm run build`.

In prose, you might mention npm run totally-made-up without backticks — that
should never be flagged since it is outside any code span or fenced block.
