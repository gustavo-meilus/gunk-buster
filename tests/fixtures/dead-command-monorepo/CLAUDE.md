# Claude instructions

Run `npm run typo` to do the thing (this script does not exist anywhere).

Install dependencies with `pnpm install` — that is a built-in subcommand, never a script.

```sh
yarn typo-in-fence
```
