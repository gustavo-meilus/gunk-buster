# Recently-modified fixture

The initial commit is backdated well outside the recency window; the test
then re-touches `dist/bundle.js` with a fresh commit at "now" to prove the
soft recency protection caps its verdict at ASK_CHIEF, never SAFE.
