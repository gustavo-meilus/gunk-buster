# pm-drift-non-node fixture

A non-Node repo. Run the build with `npm install` and `pnpm run build` — neither
check should ever fire here since there is no package.json or lockfile to
establish ground truth.
